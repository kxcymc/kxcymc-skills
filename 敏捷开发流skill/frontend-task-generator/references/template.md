# [需求名称] 前端任务拆解

> 本模板给出标准的前端开发任务清单结构。据此逐节填充，各章节顺序与要素保持一致。
> 文档面向他人阅读，读者仅凭本文档即可掌握完整改动信息。

## 需求背景（原文）

> [摘录需求文档的背景关键句，保留原文表述]

涉及页面：

- [页面/模块名称]：`[页面路由或 URL]`
- [页面/模块名称]：`[页面路由或 URL]`

需求点（逐条编号，摘录原文）：

1. [需求原文第 1 条]
2. [需求原文第 2 条]
3. [需求原文第 3 条]

## 约定

- UI 组件库与复用策略：[所用组件库、通用组件复用规则]。
- 样式方案：[样式规范，如 CSS Modules / SCSS 层叠约定]。
- 接口调用方式：[统一的请求封装、接口生成与更新流程]。
- 无设计稿的 Task 直接在代码中按现有页面视觉实现；有设计稿的 Task 调用 `ecom-d2c` skill 实现。
- 本次工作范围：[说明覆盖全部需求点，或聚焦待补齐的差异项]。

## 当前代码差异

> 列出两类结论：项目已具备的能力、仍待补齐的差异项。每条落到具体文件与代码位置，并关联对应需求条目。

- **[差异项标题]**：[现状描述，指向具体文件与代码位置]。对应需求第 [x] 条。
- **[差异项标题]**：[现状描述，例如某字段尚待消费、某组件仍为本地模式、某入口尚待接入]。对应需求第 [x] 条。
- **[已具备能力标题]**：[说明该能力已存在，本次沿用，保持现状]。

## 接口策略

> 接口就绪度依据项目内已生成的接口代码（真实方法签名、请求参数、响应字段）判断。

- **调用真实接口**：
  - `[接口方法名]`（`[service.方法]`，`[HTTP 方法与路径]`）——[接口用途；关键请求参数与响应字段]。
  - `[接口方法名]`（`[service.方法]`）——[接口用途；关键请求参数与响应字段]。
- **mock 承接**：`[接口/能力名称]` 处于开发阶段。前端以 mock 实现 [输入 → 输出] 的映射，mock 数据存放于对应组件目录 `mock.ts`，并以显式函数封装；接口就绪后替换该函数内部实现为真实调用。
- **技术选型说明**：[导出/上传/下载等能力的实现方式，依据后端方案与项目既有能力确定]。

---

## Task 1：[任务名称，动词开头，点明改动目标]

- 设计稿输入：[Figma 链接或描述；对应需求第 x 条]
- 落地文件：
  - [前端代码路径 1]
  - [前端代码路径 2]
- 数据源：[依赖的接口方法名或 mock 路径；标注关键请求/响应字段]
- 需求新增逻辑和样式：[用自然语言完整描述新增的逻辑与样式；出处以文字标注（页面名/路由/接口名/字段名/章节标题），不附链接]
- 现有逻辑和样式变更：[用自然语言完整描述被调整的逻辑与样式；出处以文字标注（页面名/路由/接口名/字段名/章节标题），不附链接]

## Task 2：[任务名称]

- 设计稿输入：[Figma 链接或描述；对应需求第 x 条]
- 落地文件：
  - [前端代码路径]
- 数据源：[接口方法名或 mock 路径]
- 需求新增逻辑和样式：[描述]
- 现有逻辑和样式变更：[描述]

## Task N：[任务名称]

- 设计稿输入：[Figma 链接或描述；对应需求第 x 条]
- 落地文件：
  - [前端代码路径]
- 数据源：[接口方法名或 mock 路径]
- 需求新增逻辑和样式：[描述]
- 现有逻辑和样式变更：[描述]

---

## 备注（记录依赖与边界）

- [处于开发阶段的接口就绪后的接入步骤：配置生成、替换 mock 等]。
- [跨端职责边界、待与后端确认的取数边界等事项]。
